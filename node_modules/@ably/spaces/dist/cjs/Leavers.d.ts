import type { SpaceMember } from './types.js';
type SpaceLeaver = {
    member: SpaceMember;
    timeoutId: ReturnType<typeof setTimeout>;
};
declare class Leavers {
    private offlineTimeout;
    private leavers;
    constructor(offlineTimeout: number);
    getByConnectionId(connectionId: string): SpaceLeaver | undefined;
    getAll(): SpaceLeaver[];
    addLeaver(member: SpaceMember, timeoutCallback: () => void): void;
    removeLeaver(connectionId: string): void;
}
export default Leavers;
