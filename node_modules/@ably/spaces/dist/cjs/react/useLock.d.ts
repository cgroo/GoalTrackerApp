import type { LockStatus, SpaceMember } from '../types.js';
interface UseLockResult {
    status: LockStatus | null;
    member: SpaceMember | null;
}
export declare function useLock(lockId: string): UseLockResult;
export {};
