export default class Logger {
    static logAction(level, action, message) {
        console.log(level, action, message);
    }
}
export var LogLevel;
(function (LogLevel) {
    LogLevel[LogLevel["LOG_ERROR"] = 0] = "LOG_ERROR";
})(LogLevel || (LogLevel = {}));
